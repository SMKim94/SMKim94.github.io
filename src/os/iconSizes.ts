/**
 * Windows 기본 아이콘 크기 (배율 100% 기준).
 *
 * 실제 Windows는 .ico 안에 16/32/48/256을 함께 담아두고 상황에 맞는 것을
 * 골라 쓴다. 우리 아이콘은 SVG라 해상도 문제는 없지만, 어디에 어떤 크기를
 * 쓰는지는 같은 규격을 따르는 편이 눈에 익다.
 */
export const ICON = {
  /** 메뉴·트레이·목록 보기·제목 표시줄 */
  small: 16,
  /** 작업표시줄, 목록의 큰 항목 */
  medium: 24,
  /** 시스템 기본 "큰 아이콘" — 시작 메뉴 고정 앱 */
  large: 32,
  /** 탐색기 "보통 아이콘", 바탕화면 기본 */
  extraLarge: 48,
} as const;
